# Set to "true" to use PostgreSQL backend instead of localStorage-only mode
VITE_USE_API=false

# API base URL (use /api in dev with Vite proxy, or full URL in production)
VITE_API_URL=/api

# Production example:
# VITE_USE_API=true
# VITE_API_URL=https://your-api.railway.app/api
